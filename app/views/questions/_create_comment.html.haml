#new_comment
  - form_for :comment, :url => {:controller => "comment", :action => "create"} do |f|
    = hidden_field_tag :sid, @question.sid
    = f.label "leave a comment"
    = f.text_field :comment
    = f.submit "submit"
