- @question.comments.each do |comment|
  - if comment.user
    .comment
      %span.user= link_to "@#{comment.user.username}", "http://twitter.com/#{comment.user.username}"
      %span.comment= h comment.comment
